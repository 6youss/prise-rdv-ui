import styled from "styled-components";
import colors from "./colors";

const H4 = styled.h4`
  color: ${colors.primary};
  font-size: 15px;
  font-weight: bold;
  padding: 0;
  margin: 0;
`;

export default H4;
