import styled from "styled-components";
import SectionWrapper from "./SectionWrapper";

const HomeSearchWrapper = styled(SectionWrapper)`
  padding-top: 40px;
  align-items:center;
  justify-content:center;
  height: 60vh;
`;

export default HomeSearchWrapper;
